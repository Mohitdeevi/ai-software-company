apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: promptcorp
  labels:
    app: promptcorp-frontend
    tier: frontend
    environment: production
spec:
  type: ClusterIP
  selector:
    app: promptcorp-frontend
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
