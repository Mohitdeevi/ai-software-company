apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: promptcorp
  labels:
    app: promptcorp-backend
    tier: backend
    environment: production
spec:
  type: ClusterIP
  selector:
    app: promptcorp-backend
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
